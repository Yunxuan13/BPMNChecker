package org.example.model;

public enum RawShape {
    // represent for shape
    // (())      ((()))    ()     {}                                                    []
    STARTEVENT, ENDEVENT, TASK, GATEWAY, SUBPROCESS, UNKNOWN
}
